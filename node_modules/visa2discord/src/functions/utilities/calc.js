module.exports = {
  evalute: (question) =>{
    if(!question) throw new Error('Question Is Missing')
    let sum = question.split('x').join("*").split('\'\'')
    const final = Math.floor(sum)
    return final
  }
}